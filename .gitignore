.venv/
tests/__pycache__/
.pytest_cache/
*.pyc
__pycache__/

# macOS system files
.DS_Store

# Packaged plugin artifacts
*.zip

# Frente 1 (GTM / strategy) + legacy — local only, never commit
gtm/
archive/
CLAUDE.local.md

# Local-only Claude config (per-machine enabled plugins, out of the versioned product)
.claude/settings.local.json

# local tool state (Claude Code browser + wrangler)
.playwright-mcp/
.wrangler/

# User-specific authority and memory substrate (generated on onboarding)
authority-context.md
memory/*.md
!memory/README.md
outputs/posts/*
outputs/strategy/*
outputs/comments/*
# Unipile credentials (never commit)
.env
.env.local

# Discovery dossiers (third-party PII)
outputs/discovery/*
!outputs/**/.gitkeep
!memory/**/.gitkeep

# Personal branding assets
assets/branding/photo-*
assets/branding/logo-*

# Local scratch / debug files
console-errs.txt
create-app.md
